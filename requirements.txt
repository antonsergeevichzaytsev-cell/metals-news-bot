# Боту как таковому сторонние пакеты не нужны — он целиком на стандартной
# библиотеке (urllib, xml, email). Единственная зависимость появилась вместе
# с переездом: планировщик вместо тринадцати крон-воркфлоу GitHub Actions.
APScheduler==3.10.4
